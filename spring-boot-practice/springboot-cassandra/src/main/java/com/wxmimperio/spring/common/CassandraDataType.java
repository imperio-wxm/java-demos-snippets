package com.wxmimperio.spring.common;

import com.datastax.driver.core.DataType;

public enum CassandraDataType {
    DATE, TIMESTAMP, BOOLEAN, DOUBLE, FLOAT, INT, BIGINT, TEXT, TIME, UUID, INET, ASCII, DECIMAL, VARCHAR, VARINT, TIMEUUID, TINYINT, SMALLINT, COUNTER
}
